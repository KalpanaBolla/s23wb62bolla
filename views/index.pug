extends layout

block content
  h1= title
  p Welcome to my second server
  button(type="button", class="btn btn-secondary btn-lg") Click here
  button(type="button", class="btn btn-danger btn-lg" onclick = "doReduce()") click here for the contents

  div(id = "reduce") Reduce the value

  script.
    var count = 20;
    var change = -1;
    function doReduce(){
      count = count + change;
      change-=1;
      document.getElementById("reduce").innerHTML = "Count "+count+"  change "+change;
    }
  a(href = "bat") bat page








































































































































































































































